# Chiave API di Google Gemini (https://aistudio.google.com/apikey).
# Serve in locale per `npm run rag:build` e per l'endpoint /api/chat in dev.
# In produzione va impostata negli Environment Variables di Vercel.
GEMINI_API_KEY=
